<?xml version="1.0"?>
<ruleset name="dead-code"
         xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0 https://pmd.sourceforge.io/ruleset_2_0_0.xsd">

  <description>
    Semantic dead-/unused-code detection for CubeWorld. Deliberately narrow: only
    rules that flag genuinely removable code, so the report stays actionable.
  </description>

  <rule ref="category/java/bestpractices.xml/UnusedPrivateMethod"/>
  <rule ref="category/java/bestpractices.xml/UnusedPrivateField"/>
  <rule ref="category/java/bestpractices.xml/UnusedLocalVariable"/>
  <rule ref="category/java/bestpractices.xml/UnusedFormalParameter"/>
  <!-- UnusedAssignment intentionally omitted: it flags side-effecting reads
       (skipping a CSV header) and defensive inits as "unused", which are not
       dead code. Keep the ruleset to genuinely removable code. -->
  <rule ref="category/java/codestyle.xml/UnnecessaryImport"/>
  <rule ref="category/java/codestyle.xml/EmptyControlStatement"/>

</ruleset>
